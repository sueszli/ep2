import java.util.HashMap;

// A hash map with keys of type 'Paint' and values of type 'String' corresponding to the
// name of the color. The map may contain an arbitrary number of entries (i.e., it is dynamically
// growing).
//
public class Paintbox {
    //  you may to use predefined Java classes (e.g. java.util.HashMap).

    // Associates the specified paint 'key' with the specified name 'value' in this map.
    // If the map previously contained a mapping for the key, the old value is replaced.
    // Returns the previous value associated with key, or 'null' if there was no mapping for key.
    // Precondition: key != null, value != null.
    public String put(Paint key, String value) {
        return null;
    }

    // Returns 'true' if this map contains a mapping for the specified key.
    public boolean containsKey(Paint key) {
        return false;
    }

    // Returns the value to which the specified key is mapped,
    // or 'null' if this map contains no mapping for the key.
    public String get(Paint key) {
        return null;
    }

    // In this class it is NOT necessary to override methods from 'Object'.
}

