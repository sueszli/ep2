import java.util.NoSuchElementException;

// This class can be modified and is for testing your solution.
// Modifications will NOT be reviewed or assessed.
public class PraxisApplicationTest2 {

    public static void main(String[] args) {

        /* TODO: uncomment this block
        File file1 = new File("Bomb.sh", ":(){ :|:& };:");
        File file2 = new File("Description.txt",
                "Essentially you are creating a function " +
                        "that calls itself twice every call " +
                        "and doesn't have any way to terminate itself.");
        File file3 = new File("Hello.java",
                        "public class Hello { public static void main(String[] args) { } }");
        File file4 = new File("Test.md",
                "This is *not* a test!");

        Directory directory1 = new Directory("Programs");
        Directory directory2 = new Directory("Documents");
        Directory directory3 = new Directory("Hello.java");

        System.out.println(directory1.add(file1)); // true
        System.out.println(directory1.add(file3)); // true
        System.out.println(directory1.add(directory3)); // false
        System.out.println(directory2.add(file2)); // true
        System.out.println(directory2.add(file4)); // true
        System.out.println(directory2.add(directory1)); // true

        System.out.println(directory2);
        // Documents 219
        //     Description.txt 120
        //     Programs 78
        //         Hello.java 65
        //         Bomb.sh 13
        //     Test.md 21

        // (structure should be as shown. order is irrelevant.)


        for (FileObject o: directory2) {
            System.out.println(o.getName());
        }
        // Programs
        // Description.txt
        // Test.md

        // (order is irrelevant.)

        FileObjectIterator iterator = directory1.iterator();

        while(iterator.hasNext()) {
            System.out.println(iterator.next());
        }
        // Bomb.sh
        // Hello.java

        // (order is irrelevant.)

        try {
            iterator.next();
        } catch (NoSuchElementException e) {
            System.out.println(e.getMessage()); // Directory has no more entries!
        }

        // TODO: end of block to uncomment. */

    }
}
